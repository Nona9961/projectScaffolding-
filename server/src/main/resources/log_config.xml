<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_PATH">./logs</Property>
    </Properties>

    <Appenders>
        <!-- 控制台输出（同步） -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- 异步文件输出 -->
        <RollingFile name="RollingFileAsync"
                     fileName="${LOG_PATH}/app.log"
                     filePattern="${LOG_PATH}/archive/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="24" modulate="true"/>
                <SizeBasedTriggeringPolicy size="500 MB"/>
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="INFO" includeLocation="true">
            <AppenderRef ref="Console"/>
        </Root>

        <AsyncLogger name="asyncFileLogger" level="INFO" includeLocation="true">
            <AppenderRef ref="RollingFileAsync"/>
            <property name="batchSize">100</property>
        </AsyncLogger>
    </Loggers>
</Configuration>
